spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${VIDEO_SERVICE_DATABASE_HOST}:${VIDEO_SERVICE_DATABASE_PORT}/${VIDEO_SERVICE_DATABASE}?autoReconnect=true&useSSL=false
    username: ${VIDEO_SERVICE_DATABASE_USER}
    password: ${VIDEO_SERVICE_DATABASE_PASSWORD}

  jpa:
    database: postgresql
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog.master.yaml

server:
  servlet:
    context-path: /video-service
  port: 8886

security:
  oauth2:
    client:
      clientId: video-service
      clientSecret: ${VIDEO_SERVICE_PASSWORD}
      accessTokenUri: http://${AUTH_SERVER_HOST:localhost}:8884/auth/oauth/token
      grant-type: client_credentials
      scope: server

spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: prod
    include: native
  security:
    user:
      password: ${CONFIG_SERVICE_PASSWORD}
server:
  port: 8888
spring:
  application:
    name: video-service
  cloud:
    config:
      uri: http://${CONFIG_SERVICE_HOST}:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
  servlet:
    multipart:
      max-file-size: 522111854
      max-request-size: 522111854

spring:
  servlet:
    multipart:
      max-file-size: 800MB
      max-request-size: 800MB
  application:
    name: video-service
  cloud:
    config:
      uri: http://${CONFIG_SERVICE_HOST}:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user




spring.datasource.url=jdbc:postgresql://${QUESTION_SERVICE_DATABASE_HOST}:${QUESTION_SERVICE_DATABASE_PORT}/${QUESTION_SERVICE_DATABASE}?autoReconnect=true&useSSL=false
spring.datasource.username=${QUESTION_SERVICE_DATABASE_USER}
spring.datasource.password=${QUESTION_SERVICE_DATABASE_PASSWORD}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:db/changelog/db.changelog.master.yaml
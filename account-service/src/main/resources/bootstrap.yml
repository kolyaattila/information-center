spring:
  application:
    name: account-service
  profiles:
    active: prod
  cloud:
    config:
      uri: http://${CONFIG_SERVICE_HOST}:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user